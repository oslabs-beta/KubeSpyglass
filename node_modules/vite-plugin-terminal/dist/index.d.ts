import { Plugin } from 'vite';

type FilterPattern = ReadonlyArray<string | RegExp> | string | RegExp | null;
type OutputType = 'terminal' | 'console';
type LogsOutput = OutputType | OutputType[];
interface Options {
    /**
     * Redirect console logs to terminal
     *
     * @default true
     */
    console?: 'terminal';
    /**
     * Output for the logs
     *
     * @default `'terminal'`
     */
    output?: LogsOutput;
    /**
     * Remove logs in production
     *
     * @default true
     */
    strip?: boolean;
    /**
     * Filter for modules to be processed to remove logs
     */
    include?: FilterPattern;
    /**
     * Filter for modules to not be processed to remove logs
     */
    exclude?: FilterPattern;
}
declare function pluginTerminal(options?: Options): (false | Plugin)[];

export { FilterPattern, LogsOutput, Options, pluginTerminal as default };
